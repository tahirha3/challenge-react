import React from 'react'

let datepicker = React.createClass({
    render() {
        return (
            <div>
                yay
            </div>
        );
    }
});

export default datepicker;